# Dependencies
Ubuntu:
```
sudo apt install make automake autoconf libsdl2-dev libsdl2-mixer-dev libsdl2-image-dev
```
# Build
```
./autogen.sh
./configure
make
```
output executable will be at src/supertux-milestone1.
